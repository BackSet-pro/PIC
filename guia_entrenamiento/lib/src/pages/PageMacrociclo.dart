import 'package:flutter/material.dart';

class MacrocicloPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MacrocicloPage'),
      ),
    );
  }
}
