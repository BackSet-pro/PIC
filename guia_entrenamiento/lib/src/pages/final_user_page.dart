import 'package:flutter/material.dart';

class FinalUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Final User Page'),
      ),
      body: Text('FinalUserPage'),
    );
  }
}
